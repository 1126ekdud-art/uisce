# CLAUDE.md

Guidance for Claude Code and other AI assistants working in this repository.

## Status: empty repository

**As of 2026-08-04 this repository contains no code.** It was created with no
initial commit — no source files, no build config, no dependency manifest, no CI.

Everything below the "Project overview" heading is a **skeleton with unfilled
placeholders**, not a description of existing code. Do not treat any placeholder
as a statement of fact about this project, and do not run a documented command
until you have confirmed it actually exists in the repo.

### What to do on the first real commit

Once the initial code lands, regenerate this file from the actual tree rather
than filling in the blanks by guesswork. Replace this whole status section with
real content, and delete any placeholder that still has nothing behind it — a
stale heading is worse than a missing one.

## Repository facts

| | |
|---|---|
| Repository | `1126ekdud-art/uisce` |
| Visibility | Public |
| Default branch | `main` |
| Language | Not yet determined |

The name *uisce* is the Irish word for "water". No stated purpose is recorded on
the repository yet; ask the user rather than inferring one from the name.

## Project overview

<!-- What this project does, who uses it, and the problem it solves. -->

_Not yet defined._

## Codebase structure

<!--
Map the top-level directories to their responsibilities once they exist, e.g.:

    src/        Application source
    tests/      Test suite
    scripts/    Developer tooling
-->

_Not yet defined — the repository has no files._

## Development workflows

<!--
Fill in the real, verified commands. Each entry should be a command that has
actually been run successfully in this repo.
-->

| Task | Command |
|---|---|
| Install dependencies | _TBD_ |
| Build | _TBD_ |
| Run locally | _TBD_ |
| Run tests | _TBD_ |
| Run a single test | _TBD_ |
| Lint / format | _TBD_ |
| Type check | _TBD_ |

## Conventions

<!--
Record only conventions observable in the code or explicitly stated by the
maintainer: naming, module layout, error handling, logging, test style,
commit message format.
-->

_Not yet established._ Until conventions exist, match the style of whatever
code is already present in the file being edited.

## Git workflow

- Default branch is `main`.
- Work on feature branches; do not commit directly to `main`.
- No CI is configured yet, so nothing validates a change automatically —
  verify changes locally before pushing.

## Notes for AI assistants

- **This file is currently mostly placeholders.** Prefer reading the actual
  files over trusting a section here that has not been filled in.
- When you add a genuinely new workflow, dependency, or convention, update the
  corresponding section in the same change — and remove its placeholder marker.
- Do not add commands, scripts, or directory descriptions to this file that you
  have not confirmed exist.
